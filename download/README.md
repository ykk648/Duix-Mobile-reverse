# 官方模型下载与分类

模型包来自 `duixcom/Duix-Mobile` 的 GitHub Releases。结构分类与哈希核对见
[`docs/release_model_catalog.md`](../docs/release_model_catalog.md)。

做 NCNN→PyTorch 转换或模型平均时，只使用 165 层新版目录；不要把
`airuike_20240409`、`bendi3_20240518` 混入新版参数池。
